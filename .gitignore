.venv*/
__pycache__/
*.py[cod]

# Generated experiment outputs and model weights
kaggle_*/
*.pt
*.safetensors
*.gguf

# Native build output
dee.cpp/build/
build/
cmake-build-*/

# Local tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local-only remote benchmark evidence.
# Allow JSONL ledger files (real_generation_ledger.jsonl) for handoff; ignore
# everything else under benchmark_reports/ (binaries, model dumps, etc.).
benchmark_reports/*
!benchmark_reports/*.jsonl
!benchmark_reports/ornith-layer0-parity-*.json

# pack-cap-ab: keep code+contract, drop heavy live outputs locally
experiment/pack-cap-ab/results/live/
